# v0.2.8

## Bug Fixes

- **Fixed right-side padding gap in terminal panes** — reduced excessive 14px width deduction in terminal size calculation to 2px, eliminating the visible dark strip between terminal content and the focus border.
- **Fill entire view with default background** — added a full-view background fill before cell-by-cell rendering so any remaining sub-pixel gaps at the right/bottom edges are covered with the terminal background color.
