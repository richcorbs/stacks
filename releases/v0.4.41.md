## Stacks v0.4.41

### Fixes

- **Reliable interface font shortcuts on macOS** — **⌥⌘+** and **⌥⌘−** now match the physical keyboard keys instead of the characters produced by Option-key translation (`±` and `–`).
