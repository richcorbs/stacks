## Changes since v0.5.2

- fix(kanban): swap detail badge colors
- fix(kanban): style detail parent badge
- feat(release): add durable scripted release pipelines
- fix(kanban): reverse project and status badge emphasis
- feat(release): add deterministic resumable workflow
- Fix GitHub release asset uploads
- fix(services): refresh commands and guard PTY stops
- fix(kanban): correct project and status badge colors
- fix(pi): preserve path completion casing
- feat(release): make stage headings disclose output
- fix(pi): respect caret boundaries for prompt history
- feat(release): reconcile retries with existing state
- Fix card badge emphasis hierarchy
