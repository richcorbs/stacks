## Features

- **Alt/Option key support**: Alt+key now sends ESC prefix (standard terminal behavior). Alt+Enter, Alt+B, Alt+F etc. work correctly in shells and editors.

## Improvements

- **Architecture refactoring**:
  - Extracted 6 dialog functions to `dialogs.zig` (-419 lines from sidebar)
  - Split `pollTick` (197 lines) into 7 focused sub-functions
  - Single `ANSI_PALETTE` constant eliminates dual color table desync risk
  - Removed unused `TerminalManager` dead code (-263 lines)
  - Cleaned up `is_active_project` parameter propagation
- **Sidebar polish**: All project names consistently bright. Removed conditional highlighting.
- **Wide character rendering**: Emoji and non-Latin symbols drawn individually at grid positions to prevent column misalignment in tables.

## Bug Fixes

- **Color palette desync**: Fixed indexed color fallback table not matching vterm palette.
