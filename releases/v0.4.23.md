## Stacks v0.4.23

### Fixes

- **Responsive workspace setup** — setup commands now run off the native UI thread so the dialog disables immediately, avoids beachballs, and keeps cancellation responsive.
- **Reliable Pi working indicator** — the animated working dots remain visible between text responses and during tool activity while STOP is available.
