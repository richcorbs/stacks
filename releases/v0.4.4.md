## Improvements

- **Cleaner ⌘ held detection** — replaced poll-based modifier checking with event-driven `flagsChanged:` on both the window (when no terminal is focused) and the terminal view (when terminal is focused). No more polling, no stale state. Badges don't appear when the app is in the background.
