## Stacks v0.4.43

### Fixes

- **Stable workspace startup directories** — navigating a shell with `cd` no longer overwrites the workspace's configured CWD.
- Live shell directories remain available for current Git context without affecting where terminals and Pi GUI panes start after a restart.
- Prevents shell navigation outside a project from disabling project-local Pi prompt and skill discovery on subsequent launches.
