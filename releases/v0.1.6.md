## Improvements

- **Sidebar header borders** — Replaced CALayer all-sides border with proper top and bottom 1px border lines, eliminating the thick right-edge artifact where the sidebar meets the main panel.

- **No project highlight on "Add Terminal" nav** — Navigating to "+ Add Terminal" with ⌘⇧[/] no longer changes the parent project's background color.

## Infrastructure

- **Reusable `addBorderLine` helper** — New `addBorderLine(view, .top/.bottom)` function for precise single-edge borders via Auto Layout–constrained subviews.
