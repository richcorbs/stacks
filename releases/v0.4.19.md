## Stacks v0.4.19

### Improvements

- **Focused diff context** — diff review now shows changes with 10 surrounding context lines instead of rendering nearly the entire file.
- **Smarter developer sidebar shortcuts** — Cmd-G focuses DIFF and Cmd-Shift-G focuses PRs, closing the sidebar only when the requested tab is already active.
