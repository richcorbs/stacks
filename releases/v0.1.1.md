# v0.1.1 — Patch

**2026-03-24**

## Bug Fixes

- **Fix crash on startup** — the auto-update checker crashed because `collectOutput` requires both stdout and stderr pipes, but stderr was set to ignore. The app would abort ~5 seconds after launch when the first update check fired.
