## Bug Fixes

- **Sidebar highlight follows dragged terminal** — When reordering a focused terminal via drag-and-drop in the sidebar, the active highlight now correctly follows the terminal to its new position. Previously the highlight stayed at the old index while the terminal remained focused.
