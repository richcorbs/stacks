## Bug Fixes

- **Readable ANSI colors on dark background**: Standard ANSI blue and other colors are now brighter and readable. Fixed a bug where two color lookup tables were out of sync — the vterm palette was updated but the indexed color fallback still had the old dark values.

## Improvements

- Custom 16-color ANSI palette optimized for dark backgrounds
- Set UTF-8 locale (`LANG`, `LC_CTYPE`) in PTY child process
